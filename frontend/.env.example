# Example environment variable for homes API used by the `useHomes` hook.
# For Vite, variables accessible to client code must start with VITE_.
# Example:
HOMES_API_URL=http://localhost:8000

# If you don't set this, the app will fall back to sample data.
